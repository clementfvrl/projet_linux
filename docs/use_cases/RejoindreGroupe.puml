@startuml

actor Utilisateur as User

top to bottom direction

usecase (Rejoindre Groupe) as join

:User: -right- join

(Se loguer) <.. join : << extends >> 

note top of  (Se loguer) : "Si pas encore logué"

(Renseigner Groupe) ..> join : << include >>
(Vérifier Groupe) ..> join : << include >>
(Ajouter utilisateur au groupe) <.. join : << extends >>

@enduml